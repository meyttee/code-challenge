import { Box, styled } from "@mui/material";

const IconWrapper = styled(Box)(() => ({
  boxShadow: "1px 1px 1px black",
  width: "20px",
  height: "20px",
  bgcolor: "grey.500",
  borderRadius: "50%",
}));

export { IconWrapper };
